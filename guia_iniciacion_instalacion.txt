OJO:
Hasta aca se espera que tenga instalado docker compose y funcionando


// Construir la imagen:
docker image build --tag laravel_web_v2 -f Dockerfile  .

//Levantar el contenedor
docker compose up

// Cuando ya este corriendo la app
docker ps
docker exec -ti ${nombre_contenedor} bash
composer install

// en caso de algun fallo, dar permisos 777 a la carpeta src 


//Modificamos el .env
DB_CONNECTION=pgsql
DB_HOST=192.168.11.233
DB_PORT=5432
DB_DATABASE=lara_test
DB_USERNAME=postgres
DB_PASSWORD=259644asdfghjkl